module.exports = function infinite_check_2 (level, rng, coeff_1, coeff_2, coeff_3, coeff_4, coeff_5, coeff_6, coeff_7, coeff_8) {

    // create infinite variable
    let infinite = 0;

    if (level === 2) {

        if (rng === 3) {

            if (coeff_3 === coeff_4) {infinite = 1} ;
        }

    }     

    return infinite;

}  